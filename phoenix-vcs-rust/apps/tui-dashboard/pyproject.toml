[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tui-dashboard---terminal-user-interface"
version = "0.1.0"
description = "Generated by Phoenix VCS formal pipeline"
requires-python = ">=3.12"
dependencies = [
  "rich",
  "textual",
]

[project.scripts]
tui-dashboard---terminal-user-interface = "app:main"

[tool.hatch.build.targets.wheel]
packages = ["."]