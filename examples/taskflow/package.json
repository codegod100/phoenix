{
  "name": "taskflow",
  "version": "0.1.0",
  "description": "Generated by Phoenix VCS — 3 services",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No lint rules configured'",
    "test": "vitest run",
    "test:watch": "vitest",
    "start:analytics": "tsc && node dist/generated/analytics/server.js",
    "start:tasks": "tsc && node dist/generated/tasks/server.js",
    "start:web-dashboard": "tsc && node dist/generated/app/server.js",
    "start": "tsc && node dist/generated/analytics/server.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  }
}
