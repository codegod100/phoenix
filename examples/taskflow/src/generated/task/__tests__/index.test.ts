import { restorTasks, list, a } from '../index.js';

describe('restorTasks', () => {
  it('should be implemented', () => {
    expect(typeof restorTasks).toBe('function');
  });
});

describe('list', () => {
  it('should be implemented', () => {
    expect(typeof list).toBe('function');
  });
});

describe('a', () => {
  it('should be implemented', () => {
    expect(typeof a).toBe('function');
  });
});

