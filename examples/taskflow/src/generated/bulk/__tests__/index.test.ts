import { bulk, a } from '../index.js';

describe('bulk', () => {
  it('should be implemented', () => {
    expect(typeof bulk).toBe('function');
  });
});

describe('a', () => {
  it('should be implemented', () => {
    expect(typeof a).toBe('function');
  });
});

